# MCP Registry Notes

Suggested server name:

```text
io.github.davidmosiah/ouramcp
```

Package:

```text
oura-mcp-unofficial
```

Primary transport:

```text
stdio
```

Primary install command:

```bash
npx -y oura-mcp-unofficial
```
